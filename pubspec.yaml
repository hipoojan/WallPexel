name: wall_pexel
description: wallpaper assignment

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.4
  google_fonts: ^2.3.1
  dio: ^4.0.5
  sizer: ^2.0.15
  get: ^4.6.1
  rive: ^0.8.4
  fluttertoast: ^8.0.9
  font_awesome_flutter: ^10.1.0
  cached_network_image: ^3.2.0
  flutter_staggered_grid_view: ^0.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/loader.riv